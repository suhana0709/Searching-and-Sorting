#Program for searching an element using Linear Search

def search(arr, x):  #Function for searching an element, where arr                      is the array and x is the number we have to                      find 
    for i in range(len(arr)):
        if arr[i] == x:
            return i
    return -1
 
#array
arr = [5, 7, 8, 3, 9, 1, 0]
x = 9
#calling function
result = search(arr, x)
print(arr)
print("Element: 9")
#output result
if result == -1:
    print("Element is not present in the array. ")
else:
    print("Element is present in the array at index: ", result)
